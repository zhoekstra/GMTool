package GMassist.framework;

import org.apache.pivot.wtk.Display;
import org.apache.pivot.wtk.Window;

public class GMassistWindow extends Window {
  
  @Override
  public void open(Display disp, Window owner) {
    super.open(disp, owner);
  }
}
